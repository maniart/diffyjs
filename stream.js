const random = Math.random;
const floor = Math.floor;

function getRandomValue(max) {
  return floor(random() * max);
}

[
  [
    [0,1,2],
    [2,3,4]
  ],
  [
    [0,1,2],
    [2,3,4]
  ],
  [
    [0,1,2],
    [2,3,4]
  ],
  [
    [0,1,2],
    [2,3,4]
  ]
]

function getRandomFrame(width, height) {
  let i = 0;
  let j = 0;
  let matrix = [];
  let row = [];
  let column;
  for(i; i < width; i++) {
    for(j; j < height; j ++) {
      column = 
      column.push(getRandomValue);
    }
    row.push(column);
  }
  return
}

function* frame() {

}
